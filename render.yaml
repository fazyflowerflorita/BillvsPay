services:
  - type: web
    name: bill-vs-pay-reconciliation
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app/server.py
    envVars:
      - key: HOST
        value: 0.0.0.0
